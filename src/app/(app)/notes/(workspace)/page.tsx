/**
 * The notes library with nothing open yet.
 *
 * The surface itself is in `layout.tsx`, which is what keeps it mounted while
 * you move between notes. This page exists to define the URL; the workspace
 * reads the selection from the pathname.
 */
export default function NotesPage() {
  return null;
}
