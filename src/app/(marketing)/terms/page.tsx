import { StaticPage } from "~/components/marketing/StaticPage";

export const metadata = { title: "Terms · KAIROS" };

export default function TermsPage() {
    return <StaticPage titleKey="termsTitle" bodyKey="termsBody" />;
}
