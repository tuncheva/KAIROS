import { StaticPage } from "~/components/marketing/StaticPage";

export const metadata = { title: "Security · KAIROS" };

export default function SecurityPage() {
    return <StaticPage titleKey="securityTitle" bodyKey="securityBody" />;
}
