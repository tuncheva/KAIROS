"use client";

/**
 * Kept as a re-export so the settings call sites read the same as before.
 *
 * The implementation this file used to hold was the most complete of the five
 * confirmation patterns in the app, so it is the one that was promoted to
 * `components/ui/ConfirmDialog` rather than replaced by it.
 */

export { ConfirmDialog } from "~/components/ui/ConfirmDialog";
